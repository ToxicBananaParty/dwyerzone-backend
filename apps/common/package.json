{
    "name": "@smarthome/common",
    "version": "1.0.0",
    "main": "dist/index.js",
    "types": "dist/index.d.ts",
    "exports": {
        ".": {
            "types": "./dist/index.d.ts",
            "default": "./dist/index.js"
        }
    },
    "files": [
        "dist"
    ],
    "type": "module",
    "scripts": {
        "build": "tsup src/index.ts --format esm --dts --out-dir dist --clean --tsconfig tsconfig.build.json",
        "test": "echo \"Error: no test specified\" && exit 1"
    },
    "keywords": [],
    "author": "Ryan Dwyer",
    "license": "ISC",
    "description": "Common types and utilities for the other apps",
    "dependencies": {
        "dotenv": "^17.2.3"
    }
}
