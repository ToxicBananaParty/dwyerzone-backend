{
  "name": "smarthome",
  "version": "1.0.0",
  "main": "index.js",
  "type": "module",
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc -b",
    "format": "prettier --log-level warn --config .prettierrc --ignore-path .prettierignore --write packages/",
    "lint": "prettier --check --log-level warn --config .prettierrc --ignore-path .prettierignore packages/ && eslint packages/ --max-warnings=0 --config eslint.config.js",
    "prepare": "husky"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "workspaces": [
    "packages/solid-snake",
    "packages/big-boss",
    "packages/the-boss"
  ],
  "devDependencies": {
    "@eslint/js": "^9.36.0",
    "eslint": "^9.36.0",
    "eslint-config-prettier": "^10.1.8",
    "eslint-plugin-prettier": "^5.5.4",
    "husky": "^9.1.7",
    "lint-staged": "^15.5.2",
    "prettier": "^3.6.2",
    "typescript": "^5.9.2",
    "typescript-eslint": "^8.45.0"
  },
  "lint-staged": {
    "*.{js,jsx,ts,tsx}": "prettier --log-level warn --config .prettierrc --ignore-path .prettierignore --write"
  }
}
